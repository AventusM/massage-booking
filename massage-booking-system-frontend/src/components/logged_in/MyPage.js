import React, { Fragment, useState, useContext } from 'react'
import { UserContext } from '../../App'
import useField from '../../hooks/useField'

const MyPage = () => {
  return <Fragment>Moi</Fragment>
}

export default MyPage
